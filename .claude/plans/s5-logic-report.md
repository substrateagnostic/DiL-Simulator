# S5 Quest/Dialog/Flag Logic Audit — TRUST ISSUES

Scope: `src/data/dialogs/index.js`, `src/data/quests/index.js`, `src/data/encounters/index.js`, `src/data/rooms/index.js`, `src/states/ExplorationState.js`, plus `DialogState.js`, `dialogGating.js`, `Room.js` (NO_BLOCK), `Player.js` (flag-set emit semantics) as supporting evidence.
Severity: **CRITICAL** = progress-blocking, **MAJOR** = reward/logic broken, **MINOR** = polish.

---

## Findings

| # | Sev | File / Location | Issue | Proposed one-line fix |
|---|-----|-----------------|-------|----------------------|
| 1 | CRITICAL | `encounters/index.js:134` (the_firm), `ExplorationState.js:308-310`, `rooms/index.js:1933` (old_vault `npcs: []`), `dialogs/index.js:4144` | **Losing to The Firm permanently blocks the game.** `the_firm_ambush` fires only from the one-shot `has_recorder_seal` flag-set listener (Player.setFlag emits only on value *change*, `Player.js:350-356`). On defeat, `retry_the_firm` is set but there is no `the_firm_retry` dialog, no Firm NPC in `old_vault`, and `vault_box_0001` node 0 short-circuits to "box is empty" without re-setting the flag. `charter_certified` becomes unobtainable → the penthouse charter gate (`ExplorationState.js:673`) blocks Act 7 forever. The objective even points to Delia, who has despawned (both her NPC entries require `notFlag: has_recorder_seal`/`delia_moved`). | In `update()` or `room-entered`, re-push `the_firm_ambush` when `currentRoom === 'old_vault' && has_recorder_seal && !defeated_the_firm` (mirroring the Rachel re-trigger pattern). |
| 2 | CRITICAL | `ExplorationState.js:184` & `:1569` (`hasAct2`), `dialogGating.js:87-94`, `dialogs/index.js:672-693` | **Skipping Alex's Act-2 chat soft-locks Act 3.** `hasAct2 = karen_defeated && !knows_server_secret` has no act ceiling. A player who never talks to Alex between Karen's defeat and `act2_complete` reaches Act 3 (quest stage 300) where the router still prefers `alex_it_act2` — but `dialogGating` caps `_act2` dialogs at stage 299, so DialogState skips every node: an *empty* dialog plays, `knows_server_secret` is never set, `alex_it_act3` is permanently shadowed, `archive_accessible` is never granted, and Act 3 cannot proceed. (The `kind: 'quest-critical'` marker in dialogGating.js is computed but never consulted.) | Add `&& !this.player.getFlag('act2_complete')` to both `hasAct2` computations (lines 184 and 1569). |
| 3 | CRITICAL | `DialogState.js:357-363` (Escape ends dialog) + `:327-329` (`read_<id>` set on abort) | **Escape aborts any dialog mid-stream and still marks it read — multiple one-shot chains can be stranded.** Non-recoverable cases: (a) `janet/alex_it/isaiah/intern` intros — Escape before the `met_*` set_flag → routing falls to `*_return` forever (read-flag set), `ready_for_ross` never derives → **Act 1 lock**; (b) `charter_challenge` — `read_charter_challenge` is set by node 0, `city_unlocked` at node 15; Escape between → city never unlocks and `_changeRoom` (line 677) never re-offers; (c) `act5_trigger` — `act5_triggered` set before push, `act4_complete` at node 8; (d) `restructuring_trio_intro`/`data_analytics_duo_intro`/`chief_restructuring_combat` — their `*_started` flags are only reset on combat *defeat* (`_handleDefeat:893-905`), not on dialog abort; (e) `the_firm_ambush` (see #1); (f) `the_firm_defeated`/`algorithm_defeated` — escaping before the final set_flag loses `charter_certified` / `algorithm_defeated`+ending choice with no replay path. | Make Escape skip-to-end only (never `_endDialog()`) — or at minimum suppress Escape-exit when the remaining nodes contain `action` nodes, and only set `read_<id>` when the end node is actually reached. |
| 4 | MAJOR | `dialogs/index.js:2194, 2213, 2232, 2251` (team_chat_hub) | **Flag read but never set: `act7_complete`.** All four ally dispatchers branch on it; the only "act7_complete" elsewhere is an *achievement id* (`AchievementManager.js:16`), not a player flag. The four Act-7 chat scenes (Janet 53-55, Alex 68-70, Isaiah 83-86, Diane 98-101) are unreachable. | Set `player.setFlag('act7_complete', true)` when `algorithm_defeated` lands (flag-set listener), or branch the hub on `algorithm_defeated` instead. |
| 5 | MAJOR | `dialogGating.js:4-12, 62` (MAIN_INTRO band 0-99), `ExplorationState.js:1511-1519` | **`met_janitor` is silently missable, locking the riddles and the Architect ending.** `janitor_intro` is only valid at quest stage < 100 (pre-briefing). If the player never visits the parking garage before talking to Ross, the intro is gated to `neutral_janitor` forever; `met_janitor` gates the entire riddle chain (`:1511`), so `janitor_riddle_3_done` — a hard requirement for the 4th ending (`algorithm_defeated` node 9) and the +2-all-stats reward — becomes unobtainable with no warning. | Remove `janitor_intro` from `MAIN_INTRO_DIALOGS` (he's optional/timeless), or drop the `met_janitor` requirement from the riddle gate. |
| 6 | MAJOR | `ExplorationState.js:1721-1722` (generic act routing) + `rooms/index.js:820` (garage janitor, no dialogId) vs `rooms/index.js:1065-1071` (archive janitor gating) | **The parking-garage janitor bypasses every archive-janitor gate.** The archive entries enforce security_guard → act3 → `janitor_needs_ross` → act4 ordering via per-entry dialogIds, but the garage janitor has no `dialogId`, so generic routing serves `janitor_act3` at act ≥ 3 (skipping the security guard beat; grants `has_archive_key`, `read_janitor_act3`) and `janitor_act4` at act ≥ 4 **without `ross_rallied`** — handing out `janitor_rallied`, `vault_code_1`, `vault_accessible`, and `hr_accessible` early. | Give the garage janitor an explicit `dialogId: 'janitor_return'` (per the CLAUDE.md routing rule: never rely on act-table fallback for the Janitor). |
| 7 | MAJOR | `rooms/index.js:1071` (janitor act6 entry), `ExplorationState.js:322-329` (`has_rolex` listener), `:1965-1989` (Act 6 objective) | **Act 6 can be skipped wholesale: the Rolex is available immediately.** `janitor_act6` is gated only on `act5_complete && !has_rolex` — not on the 5-ally/2-evidence counter the objective demands. Grabbing it instantly sets `act6_complete`, fires the unconditional toast **"Team assembled, evidence secured. +500 XP"** (possibly with 0 allies and 0 evidence), and stage-gating then locks all `_act6` rally dialogs (band 600-699) so the skipped content is gone for good. | Add `condition: { flag: 'ross_speech_ready' }` (or a derived `act6_ready` flag computed in `_refreshStoryProgress`) to the `janitor_act6` room entry. |
| 8 | MAJOR | `ExplorationState.js:1523` (janet_act4 priority) + `dialogGating.js` `_act4` band 400-499 | **Janet rally dead-end at Act 6+.** If `janet_rallied` was never set in Acts 4-5 (it is optional — only Ross+Janitor gate progression), the priority block keeps returning `janet_act4` at act ≥ 6, the stage gate rejects it (stage 600 > 499) and falls back to `neutral_janet` — forever. `janet_act6_rallied` becomes unobtainable, the "Prepare for the finale (x/5 allies)" objective can never complete, and the panel permanently nags "Rally: • Janet". | Bound the priority: `if (id === 'janet' && act >= 4 && act < 6 && !janet_rallied) return 'janet_act4';`. |
| 9 | MAJOR | `rooms/index.js:428, 491` (Ross entry: `act2_complete && !ross_returned_seen`) vs `dialogs/index.js:3147` (ross_returned content) | **Act-7 dialog reachable at Act 3.** `ross_returned` ("the Regional Director was gone… The Algorithm is already running projections") is wired to `act2_complete`, so it plays at the start of Act 3 on *every* path — including paths where no Regional anything was defeated — and name-drops The Algorithm four acts before its introduction. | Change the room condition to `{ flag: 'regional_director_defeated', notFlag: 'ross_returned_seen' }` (matching the dialog's own comment). |
| 10 | MAJOR | `Room.js:66` (`elevatorDoors` in NO_BLOCK), `rooms/index.js:764` (reception exit x:13,z:3), `ExplorationState.js:1411-1415` (on-tile exit priority) | **Executive floor reachable from Act 0.** The reception elevator exit tile is walkable (elevatorDoors don't block); standing on the exit tile bypasses `_shouldPrioritizeExit`'s `branch_chosen` check and `_changeRoom` has no `executive_floor` entry in `gatedRooms` — the "AUTHORIZED PERSONNEL ONLY" elevator dialog is decoration. Early access leaks Compliance Auditor content and exec-floor flavor in Act 1. | Add `executive_floor: { flag: 'branch_chosen', message: 'The keycard reader blinks red.' }` to `gatedRooms` (keep the Act-5 lawyer block as-is). |
| 11 | MAJOR | `ExplorationState.js:1167-1171, 1191-1194` (quarterly review) | **`portfolio_strong` is set but never read — the promised mechanic doesn't exist.** The quarterly review tells the player "Your strong portfolio gives you leverage against Rachel's restructuring arguments" / "Rachel will use your weak portfolio as evidence", but no dialog, encounter, or stat path ever consumes the flag. | Either consume it (e.g. condition node in `rachel_boss_combat` or an enemyOverride debuff) or drop the story-note copy. |
| 12 | MAJOR | `dialogs/index.js:1772-1808` (ross_act4) | **The "need 3/4 buzzwords" Ross puzzle is not implemented.** The header comment promises a 4-choice-point check; in practice every branch funnels to node 20 → `ross_rallied` unconditionally, and `ross_convince_1..4` are set but never read by anything. | Either add a condition chain counting `ross_convince_*` before node 20 (with a "come back" fail path), or delete the four choice flags and the comment. |
| 13 | MINOR | `ExplorationState.js:1506` + `rooms/index.js:244` (water-cooler Karen) | Pre-briefing Karen at the cubicle-farm water cooler is routed to `karen_intern_first` ("Go practice on the Intern") — but the Intern spar doesn't exist until `briefing_complete` (act ≥ 1), and the line presumes the Henderson meeting context. The intended `karen_intro`/`karen_return` dialogs (`dialogs/index.js:1381-1393`) are consequently unreachable. | Scope the block: `if (id === 'karen' && this.player.getFlag('briefing_complete') && !defeated_intern) …`. |
| 14 | MINOR | `rooms/index.js:751` (reception grandma) vs `:567` (conference grandma) | Grandma Henderson exists in two rooms simultaneously once `ross_post_chad` is set (reception idle requires only `chad_defeated && !grandma_defeated`; conference entry requires `ross_post_chad && !grandma_defeated`). | Add `notFlag: 'ross_post_chad'` to the reception idle entry. |
| 15 | MINOR | `rooms/index.js:428-430` (Ross entries c & e) | Conditions overlap: `(act2_complete && !ross_returned_seen)` and `(act5_complete)` are both true if the player never read `ross_returned` by Act 6 → two Ross NPCs spawn stacked on the same tile in his office (also in `ross_office_large`, lines 491-493). | Add `notFlag: 'ross_returned_seen'`-exclusivity, e.g. give entry (e) `flag: 'act5_complete', notFlag: ''`→`flag: 'ross_returned_seen'` style mutual exclusion (entry e: add `flag: 'ross_returned_seen'` is wrong for skipped players — instead add `notFlag: 'act5_complete'` to entry (c)). |
| 16 | MINOR | `rooms/index.js:921-922` (regional NPC, path_bro entry) | On `path_bro`, `defeated_regional` is never set, so the Regional Manager paces the executive floor for the rest of the game — including after Act 5's narration announces his SEC arrest. | Add `notFlag: 'act5_complete'` (or `rachel_boss` defeat flag) to both `regional` entries. |
| 17 | MINOR | `dialogs/index.js:1687-1697` (stairwell_graffiti node 3) | Fall-through bug: the pre-Act-2 branch (nodes 1-3) auto-advances into node 4 ("New graffiti has appeared since last time…"), showing the gold Fiduciary Force graffiti before Act 2 is complete — and "since last time" on a first read. | Add `next: 8` to node 3. |
| 18 | MINOR | `dialogs/index.js:1070-1072` (regional_defeated nodes 4/6) | Contradictory consecutive narration: node 4 "deploys their golden parachute … exits the building. You never see them again." then node 6 "straightens his tie. His golden parachute remains undeployed. For now." | Delete node 4 (route node 3 → 5) — node 6+ is the canonical Act-2-finale beat. |
| 19 | MINOR | `dialogs/index.js:1053-1055` (compliance_defeated nodes 3/5) | Duplicate narration: node 3 "puts their sunglasses back on and walks away" followed (after the set_flag) by node 5 "The Compliance Auditor walks away." | Drop node 5's first sentence or route node 4 → 6. |
| 20 | MINOR | `dialogs/index.js:1469` (rachel_intro), `:1630` (rachel_act3), `:1487` (rachel_return), `:2717` (janitor_dave), `:2728` (janitor_legacy_token), `:4191` (delia_epilogue), `:1848` (hr_rep_intro), `:305-306` (vending nodes 12-13), `:2785` (alex_it_quest_printer node 8) | **Unreachable content:** no Rachel NPC exists before the board room (Act 5), so her intro/act3/return dialogs (and `met_rachel`) never fire — even though `janet_act3` tells the player Rachel is on the exec floor; `janitor_dave`/`janitor_legacy_token` are referenced by no NPC/interactable/routing; `delia_epilogue` has no NPC entry once `has_recorder_seal` hides Delia; `hr_rep_intro` is bypassed by the room entries; vending "OUT OF WISDOM" nodes and the printers_soul stage-2 `quest_update` node are orphaned. | Either wire them up (Rachel exec-floor NPC for Act 3 with `condition: {flag: 'act2_complete', notFlag: 'act4_complete'}`; Delia epilogue entry on `charter_certified`; route janitor_dave/legacy_token from the Janitor during the respective subquests) or delete. |
| 21 | MINOR | `rooms/index.js:1145` (hr_rep defeated entry) | The post-victory HR rep NPC replays the full `hr_rep_defeated` post-combat dialog (incl. "you search the files" narration and the vault-code reveal) on every interaction, forever. | Point the entry at a short `hr_rep_intro`-style return line (nodes 6-7 of `hr_rep_intro` already exist for exactly this). |
| 22 | MINOR | `ExplorationState.js:650-660` (gatedRooms) vs `rooms/index.js:1899` (old_branch → old_vault exit), `dialogs/index.js:4132-4138` (barista) | `old_vault` is not in `gatedRooms`: the basement is enterable before `delia_moved` even while Jules says "Staff only … No, you can't." Soft-gated only by box 0001 needing the key, so it's a flavor/sequence leak rather than a break. | Add `old_vault: { flag: 'delia_moved', message: "Jules angles between you and the basement door. Staff only." }`. |
| 23 | MINOR | dialogs (various) | **Orphan flags — set but never read** (excluding auto `defeated_/retry_/bestiary_/read_` patterns): `cooler_exhausted`, `fridge_saga_complete`, `grandma_met`, `secret_path_complete`, `met_rachel`, `rachel_lockdown`, `isaiah_friendly`, `isaiah_act3_allied`, `has_archive_key`, `ross_convince_1..4` (#12), `portfolio_strong` (#11), `cfos_assistant_defeated` (Act-5 duo post-dialog — note it shares its name with the *penthouse* post-dialog id, inviting future confusion), `brand_consultant_fight_started`/`restructuring_fight_started` (written by the trio victory dialog, consumed only by the defeat-reset loop that can never see them set-without-victory). **Read but never set:** `act7_complete` (#4). | Prune or wire; at minimum rename `cfos_assistant_defeated` (flag) vs `cfos_assistant_defeated` (dialog id). |
| 24 | MINOR | `rooms/index.js:359` (break-room grandma) + `ExplorationState.js:1734` | At act ≥ 6 (post-`act6_complete`), stage-gating retires `grandma_act6`, so routing falls back to the unread `grandma_intro`: Grandma in the break room says "Meet me in the conference room when you're ready" — four acts stale — and hands out a free full `heal` once. | Add a `grandma_return` dialog so the `${id}_return` fallback (line 1735) catches her after act 6. |
| 25 | MINOR | `ExplorationState.js:2173-2188` (`_getActiveSideQuestHints`) | Riddle hint fires from `met_janitor` alone: from Act 1 the HUD says "The Janitor has a riddle for you — find him in the Archive", but the Archive is sealed until Act 3 and riddles don't unlock until `read_janitor_act3`. | Gate the hint on `read_janitor_act3` (the actual riddle unlock). |
| 26 | MINOR | `dialogs/index.js:2065` (alex_badge_audit_offer node 6) | Dialog references a room by raw/wrong id: "IT closet is off the it_office" — there is no `it_office` room; PATCH-3 lives in the Server Room. | Reword to "rack PATCH-3 in the server room". |
| 27 | MINOR | `rooms/index.js:754` (reception corporate_lawyer), `ExplorationState.js:663-668`, `:2004-2006` | Dead solo-gauntlet remnants: the trio post-dialog sets `restructuring_defeated` and `corporate_lawyer_defeated` in the same frame, so the reception lawyer NPC condition can never be true, the exec-floor "elevator won't open" block never engages, the `'Push through to reception'` objective line is unreachable, and `corporate_lawyer_combat`/`corporate_lawyer_defeated` solo dialogs are dead. Harmless today, but a trap if the trio is ever re-split. | Delete the solo path (NPC entry, gate, objective line) or comment it as intentionally vestigial. |
| 28 | MINOR | `dialogs/index.js:1109-1118` (andrews_desk node 0) | After `branch_chosen`, interacting with Andrew's desk produces zero output (`grandma_defeated → ifTrue: 8 → end` with no text once `_getInteractableDialogId` stops redirecting to `branch_decision`). | Point `ifTrue` at node 7 ("Your desk. Your life now…") instead of 8. |
| 29 | MINOR | `dialogs/index.js:1013` (ross_post_chad node 8/10) | Ross calls Grandma's late husband "Gerald"; everywhere else (grandma_meeting, secret_ending) he is "Harold" — possibly an intentional Ross-is-wrong gag, but it collides with the *other* Gerald (Hitchcock, Tuesday-2PM quest). | If intentional, have Andrew correct him ("…Harold?"); otherwise rename to Harold. |

---

## Act 6½ chain verification (task item 5)

`charter_challenge` → `records_clerk_form11c` → `delia_booth4` → `barista_vault` → `vault_box_0001` → `the_firm` → `the_firm_defeated` → `charter_certified`:

- **Trigger**: penthouse entry with `act6_complete && !charter_certified` fires the toast + `charter_challenge` (sets `city_unlocked`, garage exit gated correctly via `gatedRooms.city_street`). ✅ — *but* abortable with Escape after node 0 → permanent lock (#3b).
- **Clerk**: Form 11-C retry pattern correct (`form_11c_attempted` gate at nodes 0-2; wrong forms loop back; 150 XP once). ✅
- **Delia**: wrong-answer retry via `met_delia` gate works; `delia_moved` relocates her (Lucky's entry `notFlag: delia_moved`, Roastery entry `delia_moved && !has_recorder_seal` — no double-spawn). ✅
- **Barista**: dialog-only gate; `old_vault` exit is NOT in `gatedRooms` (#22). Box itself is key-gated on `delia_moved`, so no hard break. ⚠
- **Box 0001**: sets `has_recorder_seal` → flag-set listener queues `the_firm_ambush` after the box dialog ends. ✅
- **The Firm**: victory → `the_firm_defeated` sets `charter_certified` (+400 XP, achievement via `defeated_the_firm`). **Loss or dialog-abort = permanent dead end** (#1, #3) — no retry dialog, no NPC, no re-trigger; objective then points at a despawned Delia. ❌

## Boss retry audit (task item 6)

| Encounter | Retry path | OK? |
|---|---|---|
| karen / chad / grandma / compliance / regional / ross_boss / intern | `*_retry` dialogs + NPC/room re-triggers (`_getDialogId:1496`, exec-floor room-entered) | ✅ |
| hr_rep | room-entered re-fires until `defeated_hr_rep` | ✅ |
| security_guard (archive) | one-shot room trigger, but NPC entry (`notFlag: security_guard_info`) allows re-fight | ✅ |
| restructuring_trio / data_analytics_duo / chief_of_restructuring / rachel_boss | `_handleDefeat` resets `*_started` flags; room/update re-triggers | ✅ (combat defeat only — dialog-abort not covered, #3d) |
| cfos_assistant / regional_director | penthouse NPC entries (`notFlag: cfos_defeated` / `cfos_defeated && !regional_director_defeated`) | ✅ |
| algorithm | `algorithm_terminal` interactable fight path | ✅ |
| patch_defense | re-route via `alex_it_quest_final` node 8 | ✅ |
| parking_enforcer / networking_guy | NPCs persist until done-flags | ✅ |
| **the_firm** | **none** | ❌ #1 |

## Count summary

- **CRITICAL: 3** (#1 The Firm loss dead-end, #2 Alex act-2 shadow blocks Act 3, #3 Escape-abort systemic hole)
- **MAJOR: 9** (#4-#12)
- **MINOR: 17** (#13-#29)
- **Total: 29 findings** (plus 13 orphan write-only flags and 1 read-never-set flag itemised in #23/#4)

Highest-leverage fixes: #3 (one DialogState change neutralises an entire class of soft-locks), #1 and #2 (one-line condition fixes each), #6/#7 (two room-data condition edits restore Act 4/6 sequencing).
